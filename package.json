{
  "name": "vo2max",
  "version": "0.9.26",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/sync-version.cjs",
    "build": "tsc",
    "check": "tsc --noEmit",
    "serve": "npx serve .",
    "deploy": "build-deploy.bat"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}