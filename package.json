{
  "name": "sisu-trainer",
  "version": "0.9.11",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/sync-version.cjs",
    "build": "tsc",
    "prebuild:native": "node scripts/sync-version.cjs",
    "build:native": "node scripts/build-native.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test tests/machineGuidance.test.mjs",
    "cap:sync": "npm run build:native && npx cap sync && node scripts/normalize-ios-spm.mjs",
    "cap:sync:android": "npm run build:native && npx cap sync android",
    "cap:sync:ios": "npm run build:native && npx cap sync ios && node scripts/normalize-ios-spm.mjs",
    "android": "npm run cap:sync:android && npx cap open android",
    "ios": "npm run cap:sync:ios && npx cap open ios",
    "serve": "npx serve .",
    "deploy": "build-deploy.bat"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "esbuild": "0.28.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "8.3.0",
    "@capacitor-community/keep-awake": "8.0.1",
    "@capacitor-community/text-to-speech": "8.0.2",
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/ios": "8.5.0"
  }
}